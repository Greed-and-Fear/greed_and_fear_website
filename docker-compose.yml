version: '3.6'
services:
  discord:
    image: website
    container_name: website_container
    ports:
      - "80:80"
    restart: always
    extra_hosts:
      - "host.docker.internal:host-gateway"
    expose:
      - "80"
    volumes:
      - .:/root/www/
    tty: true
